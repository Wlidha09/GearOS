<Window x:Class="GearOS.MappingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configuration du périphérique" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen" Background="#121212">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="180"/>
		</Grid.RowDefinitions>

		<TextBlock x:Name="DeviceNameText" Text="TARTARUS V2"
                   Foreground="White" FontSize="24" FontWeight="Bold"
                   Margin="20" HorizontalAlignment="Center"/>

		<Grid Grid.Row="1" Margin="20">
			<Image Source="/Assets/tartarus_bg.png" Stretch="Uniform" Opacity="0.5"/>

			<ItemsControl x:Name="KeysItemsControl">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<Canvas IsItemsHost="True" />
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>

				<ItemsControl.ItemContainerStyle>
					<Style TargetType="ContentPresenter">
						<Setter Property="Canvas.Left" Value="{Binding X}" />
						<Setter Property="Canvas.Top" Value="{Binding Y}" />
					</Style>
				</ItemsControl.ItemContainerStyle>

				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<Button Width="38" Height="38"
                                Content="{Binding ID}"
                                Click="Key_Click"
                                Background="#2A2A2A" Foreground="White"
                                BorderBrush="#FF00FF" BorderThickness="1">
							<Button.Resources>
								<Style TargetType="Border">
									<Setter Property="CornerRadius" Value="4"/>
								</Style>
							</Button.Resources>
						</Button>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
		</Grid>

		<Border Grid.Row="2" Background="#1A1A1A" BorderBrush="#333" BorderThickness="0,1,0,0">
			<Grid Margin="20">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="250"/>
					<ColumnDefinition Width="*"/>
				</Grid.ColumnDefinitions>

				<StackPanel Margin="0,0,20,0">
					<TextBlock Text="TYPE D'ACTION" Foreground="Gray" Margin="0,0,0,10" FontSize="11"/>
					<ComboBox x:Name="ActionTypeCombo" Height="35" Background="#252525" Foreground="White"
                              SelectionChanged="ActionTypeCombo_SelectionChanged">
						<ComboBoxItem Content="Macro (Séquence)"/>
						<ComboBoxItem Content="Remap (Touche)"/>
					</ComboBox>
				</StackPanel>

				<StackPanel Grid.Column="1">
					<TextBlock Text="SÉQUENCE DE MACRO (Aperçu instantané)" Foreground="Gray" Margin="0,0,0,10" FontSize="11"/>
					<TextBox x:Name="MacroInput" Height="40" Background="#252525" Foreground="White" Padding="10,0" 
                             VerticalContentAlignment="Center" BorderThickness="1" BorderBrush="#444"
                             TextChanged="MacroInput_TextChanged"/>
				</StackPanel>
			</Grid>
		</Border>
	</Grid>
</Window>